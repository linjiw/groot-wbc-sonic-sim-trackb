# Test fixtures

Fixtures in this directory are for schema and pipeline tests only.

`sonic_vla_tiny` is generated during tests by
`scripts/research/create_tiny_sonic_vla_fixture.py`. It contains synthetic
numeric rows and placeholder videos. It is **not** suitable for GR00T training,
model evaluation, or robotics performance claims.
