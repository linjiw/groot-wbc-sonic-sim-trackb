# Autoresearch: LFH 3D motion–scene loop and full-simulation evidence

Goal: turn the narrow overhead LFH demonstration into an honestly scoped motion-generation →
trajectory-conditioned 3D scene proposal → SONIC/physics validation loop, and render the complete
G1 simulation with an externally verifiable camera.

Metrics: fresh motion provenance; 3D proposal axes and finite extents; physics-only verdict;
binding attribution before drift; robot/obstacle/route camera coverage; reproducible page assets.

Verify: Kimodo generation record, LFH CPU proposal/keepout, hash-pinned simulation run, camera
coverage checks, Isaac/MuJoCo video inspection, focused tests, Black, Ruff, and Sphinx.

Safety: no push, publish, protected-evaluation mutation, new edit operator, or learned physics
verdict model.

Outcome: the fresh generated motion transaction failed closed before scene authoring because its
crouch twin did not track. A separate seed-matched E10b pilot verified a five-obstacle Isaac scene
with one causal binding panel and four non-causal route-relative context obstacles. The wide-camera
videos and measured 2D/3D maps now render on the LFH Sphinx/GitHub Pages source. Critical support is
still overhead-only; lateral, floor, and oblique normals are representational, not verified.
