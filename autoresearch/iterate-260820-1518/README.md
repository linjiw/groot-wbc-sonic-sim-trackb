# LFH Autonomous Phase-2 Physics

- Objective: close the accepted Phase-2 bookkeeping gaps, self-authorize the reviewed manifests
  under `docs/hallucination/GOVERNANCE.md`, and execute the physics sequence without disturbing
  frozen or claim-5 inputs.
- Primary metrics: explicit four-sign certificates on all regenerated pairs; explicit prediction
  or abstention on every plane probe; zero E1a source-outcome flips; serial execution within the
  8 contended GPU-hour daily budget.
- Safety boundary: registered, hash-pinned physics only; one rollout at a time behind the 9000 MiB
  free-memory gate. Stop on an E1a flip, protected-input disturbance, infrastructure failure, or
  work requiring a new operator/paper claim. No push, publish, or deployment.

See `results.tsv` for keep/discard decisions.
